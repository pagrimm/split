//Expenses = {this.total = num; credits = [], debits = []}

export class Expense {
  constructor(total, credits, debits) {
    this.total = total;
    this.credits = credits;
    this.debits = debits;
  }
}